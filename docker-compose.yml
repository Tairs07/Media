version: '3.8'

services:
  # PostgreSQL 数据库
  postgres:
    image: postgres:18-bookworm
    container_name: mediashare-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=mediashare
      - POSTGRES_USER=mediashare_user
      - POSTGRES_PASSWORD=mediashare_password
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - mediashare-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mediashare_user -d mediashare"]
      interval: 10s
      timeout: 5s
      retries: 5

  # 后端服务
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: mediashare-backend
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      # 持久化上传文件
      - ./backend/wwwroot/uploads:/app/wwwroot/uploads
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:5000
      - ConnectionStrings__DefaultConnection=Host=postgres;Port=5432;Database=mediashare;Username=mediashare_user;Password=mediashare_password
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - mediashare-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # 前端服务
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - VITE_API_BASE_URL=http://your-server-ip:5000
    container_name: mediashare-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - mediashare-network

networks:
  mediashare-network:
    driver: bridge

volumes:
  postgres-data:
    driver: local
